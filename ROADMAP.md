# Roadmap

<!-- generated by cairn; edit the files in `cairn/items` and run `cairn render` -->

## v0.1 — Dogfood

`#####·····` 43% · 3 of 7 done · due 2026-09-26

Install brainiac, point it at a repo you own, and get a context pack worth pasting — from the CLI, the TUI, or an agent over MCP.

### backlog

- [ ] `0009` Build and test on macOS and Linux in CI <sup>chore · p1 · build</sup>
- [ ] `0010` cargo install from a clean clone works with no extra steps <sup>chore · p0 · build</sup>
- [ ] `0011` Verify the README quickstart verbatim, as a stranger <sup>docs · p0 · docs</sup>

### in progress

- [ ] `0008` Document MCP registration and verify it in a real agent <sup>docs · p0 · mcp</sup>

### done

- [x] `0006` Publish the repository and get the first CI run green <sup>chore · p0 · build</sup>
- [x] `0007` A concurrent commit kills an index run instantly <sup>bug · p0 · store</sup>
- [x] `0013` Indexing must scope to the directory named, not the whole enclosing repo <sup>bug · p1 · index</sup>

## v0.2 — Trust it

`··········` 0% · 0 of 19 done · due 2026-10-24

See why brainiac ranked something where it did, steer it when it is wrong, and know that a ranking change made results better rather than merely different.

### backlog

- [ ] `0012` A binary file with a source extension must not disappear silently <sup>bug · p1 · index</sup>
- [ ] `0014` How does brainiac behave on a five-million-line repository? <sup>spike · p1 · index</sup>
- [ ] `0015` How do we know a ranking change made results better rather than different? <sup>spike · p0 · rank</sup>
- [ ] `0016` Build the ranking evaluation harness <sup>feature · p0 · rank</sup>
- [ ] `0017` Fail the check when ranking quality drops below baseline <sup>chore · p0 · build</sup>
- [ ] `0018` Per-repo configuration in .brainiac.toml <sup>feature · p0 · config</sup>
- [ ] `0019` Explain the per-signal contribution behind every hit <sup>feature · p1 · rank</sup>
- [ ] `0020` Filter search and pack by path prefix <sup>feature · p1 · cli</sup>
- [ ] `0021` Pin specific files into a pack with --include <sup>feature · p1 · pack</sup>
- [ ] `0022` Skip re-hashing files whose mtime and size are unchanged <sup>feature · p1 · index</sup>
- [ ] `0023` Retune the priors against the evaluation set <sup>chore · p1 · rank</sup>
- [ ] `0024` brainiac symbol <name> on the CLI <sup>feature · p2 · cli</sup>
- [ ] `0025` JSON output for the commands worth scripting <sup>feature · p2 · cli</sup>
- [ ] `0026` Golden-file tests for pack layout <sup>chore · p2 · pack</sup>
- [ ] `0027` Index administration: where it is, how big, how to remove it <sup>feature · p2 · cli</sup>
- [ ] `0028` Open the selected hit in $EDITOR from the browser <sup>feature · p2 · tui</sup>
- [ ] `0029` Every error names the path and what to do about it <sup>chore · p1 · cli</sup>
- [ ] `0030` Document .brainiac.toml <sup>docs · p1 · docs</sup>
- [ ] `0065` Scoping into an ignored directory indexes ignored content <sup>bug · p2 · index</sup>

## v0.3 — Reach

`··········` 0% · 0 of 14 done · due 2026-11-21

Handle the repositories you actually have — monorepos, more languages, prose with structure, files too big for the cap — and stay fast doing it.

### backlog

- [ ] `0031` Index a monorepo once and query it per package <sup>feature · p0 · index</sup>
- [ ] `0032` Chunk prose by heading rather than by sliding window <sup>feature · p1 · parse</sup>
- [ ] `0033` Java symbol graph <sup>feature · p1 · parse</sup>
- [ ] `0034` C and C++ symbol graphs <sup>feature · p1 · parse</sup>
- [ ] `0035` Ruby symbol graph <sup>feature · p2 · parse</sup>
- [ ] `0036` Verify every language against a real repository, not a fixture <sup>chore · p0 · parse</sup>
- [ ] `0037` Chunk files over the size cap instead of skipping them <sup>feature · p2 · index</sup>
- [ ] `0038` Report what was skipped and why <sup>feature · p2 · cli</sup>
- [ ] `0039` Cache git churn instead of spawning git on every index run <sup>chore · p2 · index</sup>
- [ ] `0040` Batch index writes so one huge transaction does not hold the database <sup>chore · p1 · store</sup>
- [ ] `0041` Resolve imports so graph edges point at the right file <sup>feature · p0 · rank</sup>
- [ ] `0042` Is a file watcher worth it once re-hashing is skipped? <sup>spike · p2 · index</sup>
- [ ] `0043` Filter the browser by path and area <sup>feature · p2 · tui</sup>
- [ ] `0044` Return structured results from the MCP tools <sup>feature · p2 · mcp</sup>

## v1.0 — The promise

`··········` 0% · 0 of 13 done · due 2027-01-16

Ask a question about a repository; get back a token-budgeted pack of the code most likely to answer it. True on macOS and Linux, documented, and stable enough that breaking it is a bug.

### backlog

- [ ] `0045` Freeze and document the CLI surface <sup>docs · p0 · cli</sup>
- [ ] `0046` Freeze and version the MCP tool schemas <sup>chore · p0 · mcp</sup>
- [ ] `0047` Audit every failure path end to end <sup>chore · p0 · cli</sup>
- [ ] `0048` A corrupt or locked index recovers by rebuilding <sup>feature · p0 · store</sup>
- [ ] `0049` Verify Linux parity beyond the test suite <sup>chore · p0 · build</sup>
- [ ] `0050` Architecture page: what L, S and G actually do <sup>docs · p0 · docs</sup>
- [ ] `0051` Tuning guide: budgets, excludes, and when to reindex <sup>docs · p1 · docs</sup>
- [ ] `0052` Quality gate green on every evaluation repository <sup>chore · p0 · rank</sup>
- [ ] `0053` Document and enforce performance budgets <sup>chore · p1 · build</sup>
- [ ] `0054` Tagged releases with checksums <sup>chore · p1 · build</sup>
- [ ] `0055` A schema bump rebuilds visibly, not silently <sup>feature · p1 · store</sup>
- [ ] `0056` Dependency audit in CI <sup>chore · p2 · build</sup>
- [ ] `0057` Man page and complete help for every subcommand <sup>docs · p2 · cli</sup>

## later — Later

`··········` 0% · 0 of 7 done

Nothing. This is where good ideas wait instead of being argued about inside a milestone that has a date.

### backlog

- [ ] `0058` Would embeddings actually beat the L/S/G fusion on my repositories? <sup>spike · p3 · rank</sup>
- [ ] `0059` LSP-backed symbol resolution <sup>feature · p3 · rank</sup>
- [ ] `0060` Watch mode or a background daemon <sup>feature · p3 · index</sup>
- [ ] `0061` Search across several repositories at once <sup>feature · p3 · cli</sup>
- [ ] `0062` Editor integrations <sup>feature · p3 · cli</sup>
- [ ] `0063` Windows support <sup>feature · p3 · build</sup>
- [ ] `0064` Rerank the top candidates with a stronger model <sup>feature · p3 · rank</sup>

